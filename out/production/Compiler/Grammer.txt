/****program may have any number of if-else and any number of assignment or declaring Variables or identifier ****\
// Main function
int main(){
    int x=5+6;
       int ksd_f5878 = 5 + 7
       int ksd_f5878=5+7
       int x = 5 + 6;
       if(x){
           sdds;
       }
}

// if statement with else and else is necessary for every if it must be exist (there is no if without else)
if (condition){
}
else{
}

//condition
 >,<,>=,<=,==,!= and ()

// Data types
int = 4 byte to 10^5
float = 4 byte
char = 1 byte (just one alphabet character or one digit or one whitespace) no symbols

// Assignment or variable declaration
datatype identifier = value or another identifier or expression

// Expression
+,-,*, /, ()
() has most priority then *,/ then +,-

// print function
cout << identifier